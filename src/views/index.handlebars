<h1>Practica Integradora</h1>

{{#each images}}
    <div class="card">
        <img src={{this.path}} alt="">
        <h3> {{this.title}} </h3>
        <p> {{this.description}} </p>
        <a href="/image/{{this.id}}/delete" class="btn"> Eliminar </a>
    </div>
{{/each}}
